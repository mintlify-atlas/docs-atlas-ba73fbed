---
title: List Categories
api: GET /categories
---

Retrieves a list of all categories in the system, ordered by creation date (newest first).

## Response

<ResponseField name="data" type="Category[]">
  Array of category objects
  
  <Expandable title="Category object">
    <ResponseField name="id" type="number" required>
      Unique identifier for the category
    </ResponseField>
    
    <ResponseField name="name" type="string" required>
      Category name
    </ResponseField>
    
    <ResponseField name="slug" type="string" required>
      URL-friendly slug, automatically generated from name
    </ResponseField>
    
    <ResponseField name="isActive" type="boolean" required>
      Whether the category is active
    </ResponseField>
    
    <ResponseField name="createdAt" type="string" required>
      ISO 8601 timestamp of when the category was created
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl --request GET \
  --url https://api.example.com/categories
```
</RequestExample>

<ResponseExample>
```json 200
[
  {
    "id": 1,
    "name": "Teas",
    "slug": "teas",
    "isActive": true,
    "createdAt": "2024-03-10T12:00:00.000Z"
  },
  {
    "id": 2,
    "name": "Pastries",
    "slug": "pastries",
    "isActive": true,
    "createdAt": "2024-03-09T15:30:00.000Z"
  }
]
```
</ResponseExample>
